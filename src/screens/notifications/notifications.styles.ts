import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  postPhoto: {
    width: 50,
    height: 50,
    margin: 8,
    borderRadius: 10,
    marginLeft: "auto",
  },
  notificationBlock: {
    flexDirection: "row",
    paddingLeft: 10,
    alignItems: "center",
    gap: 10,
  },
  date: {
    fontSize: 8,
  },
});

export default styles;
