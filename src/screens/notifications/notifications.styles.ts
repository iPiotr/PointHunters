import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  postPhoto: {
    width: 50,
    height: 50,
    margin: 8,
    borderRadius: 10,
    marginLeft: "auto",
  },
  notificationBlock: {
    paddingLeft: 10,
  },
});

export default styles;
